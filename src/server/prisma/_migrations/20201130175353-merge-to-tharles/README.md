# Migration `20201130175353-merge-to-tharles`

This migration has been generated by luizmlpascoal at 11/30/2020, 2:53:53 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "User" (
"id" SERIAL,
    "name" TEXT,
    "email" TEXT,
    "city" TEXT NOT NULL,
    "state" TEXT NOT NULL,
    "country" TEXT NOT NULL,
    "organizationId" INTEGER NOT NULL,
    "geeKey" TEXT NOT NULL,
    "password" TEXT NOT NULL,
    "typeUserInCampaign" "TypeUserInCampaign" NOT NULL DEFAULT E'INSPETOR',
    "typeUser" "TypeUser" NOT NULL DEFAULT E'USER',
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,
    "active" BOOLEAN NOT NULL DEFAULT false,

    PRIMARY KEY ("id")
)

CREATE UNIQUE INDEX "User.email_unique" ON "User"("email")
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201125180031-enum-types..20201130175353-merge-to-tharles
--- datamodel.dml
+++ datamodel.dml
@@ -3,27 +3,34 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 model User {
   id                 Int                @id @default(autoincrement())
   name               String?
-  email              String             @unique
-  age                Int
-  address            String?
+  email              String?            @unique
+  city               String
+  state              String
+  country            String
+  organizationId     Int
+  geeKey             String
+  password           String
   typeUserInCampaign TypeUserInCampaign @default(INSPETOR)
   typeUser           TypeUser           @default(USER)
+  createdAt          DateTime           @default(now())
+  updatedAt          DateTime           @updatedAt
+  active             Boolean            @default(false)
 }
+enum TypeUser {
+  USER
+  SUPERADMIN
+}
+
 enum TypeUserInCampaign {
   INSPETOR
   SUPERVISOR
   ADMIN
 }
-
-enum TypeUser {
-  USER
-  SUPERADMIN
-}
```


